version: '3.8'
services:
  app:
    build: .
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - ENV=production
      - LOG_LEVEL=INFO
    command: python -m src.main